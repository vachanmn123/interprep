import type {
  Test,
  TestAttempt,
  TestAttemptAnswer,
  QuestionOption,
  Question,
  TestType,
  QuestionType,
} from "@prisma/client";

export type {
  Test,
  TestAttempt,
  TestAttemptAnswer,
  QuestionOption,
  Question,
  TestType,
  QuestionType,
};
